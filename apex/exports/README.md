# Exports APEX

Colocar aquí los resultados de `apex export` (full o split).

Convención sugerida:

```text
exports/app-<ID>-split/...
exports/app-<ID>-full.sql
```

No añadir al repositorio exports con **datos sensibles** en colecciones o archivos estáticos sin revisión.
