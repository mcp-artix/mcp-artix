# Reverse proxy (opcional)

## Propósito

Unificar hostnames locales y TLS de demostración. **Plantilla**: no está cableado en el `docker-compose.yml` maestro de esta iteración.

## Guía

[../../docs/instalacion/07-reverse-proxy-opcional.md](../../docs/instalacion/07-reverse-proxy-opcional.md)

## Próximos pasos

Añadir servicio `caddy` o `traefik` con perfil `proxy` y volúmenes para `Caddyfile`/`dynamic.yml`.
