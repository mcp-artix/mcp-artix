# Servidores MCP

| Proyecto | Descripción |
|----------|-------------|
| [oracle-apex/](oracle-apex/) | Oracle, APEX, ORDS, Git — tools en **stub** salvo evolución |
| [glpi/](glpi/) | GLPI tickets e ITIL — tools en **stub** |

Instalación y ejecución: [../docs/instalacion/08-servidores-mcp.md](../docs/instalacion/08-servidores-mcp.md).

Tras clonar, en cada carpeta: `npm install && npm run build` (el directorio `dist/` no se versiona; está ignorado por `.gitignore`).
