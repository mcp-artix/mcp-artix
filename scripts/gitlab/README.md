# Scripts GitLab (servidor dedicado)

| Script | Uso |
|--------|-----|
| [preflight-gitlab.sh](preflight-gitlab.sh) | Comprueba que `GITLAB_SERVER_URL` esté definida en `infra/.env` y muestra el comando orientativo para registrar el Runner |

Variables: ver [../../infra/.env.example](../../infra/.env.example) (`GITLAB_SERVER_URL`, `GITLAB_REMOTE_SSH_HOST`).

Documentación: [../../docs/instalacion/04-gitlab-servidor-dedicado.md](../../docs/instalacion/04-gitlab-servidor-dedicado.md).
